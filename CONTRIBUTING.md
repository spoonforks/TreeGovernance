# Contributing

1. Identify the source or empirical basis.
2. Update `data/model.json`.
3. Run `npm run check`.
4. Mark new relations as `documented` or `synthesis`.

Use stable IDs, preserve Dutch institutional names, add an English `translation` where useful, and avoid orphaned nodes inside curated lenses.
